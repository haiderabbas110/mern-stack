Hoisting
Closure
type of scopes
Typed or interface
Tuples
debounce
Throttle

//- Utility function: that does not have any side effect means that do not have some effect anywhere
let utility = (x) => {
  return Math.pow(x,3);
}
